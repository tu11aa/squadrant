// Re-exported from @cockpit/shared — types live there now.
export type {
  WorkspaceRef,
  PaneRef,
  RuntimeSpawnOptions,
  PanePlacement,
  RuntimePaneOptions,
  RuntimeProbeResult,
  RuntimeDriver,
} from "@cockpit/shared";

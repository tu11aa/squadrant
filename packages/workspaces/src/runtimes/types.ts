// Re-exported from @squadrant/shared — types live there now.
export type {
  WorkspaceRef,
  PaneRef,
  RuntimeSpawnOptions,
  PanePlacement,
  RuntimePaneOptions,
  RuntimeProbeResult,
  RuntimeDriver,
} from "@squadrant/shared";

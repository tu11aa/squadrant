---
name: gate
description: Shortcut for squadrant:set-gate — get or set the permission gate mode (on | off | auto). Use when the user types /gate or asks about the permission gate.
---

# gate — shortcut for squadrant:set-gate

Shorthand alias. **Invoke the `set-gate` skill** (via the Skill tool) and follow it exactly. All logic lives there — this file intentionally holds none, so the two never drift.
